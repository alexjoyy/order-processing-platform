server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/payment_db
    username: payment_user
    password: payment_pass
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health,info

security:
  demo:
    username: alexjoy
    password: change-me
  jwt:
    secret: alexjoy-order-platform-secret-key-1234567890-super
    expiration-seconds: 3600
